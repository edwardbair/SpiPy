# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py39, py310, py311, py312


[testenv]
package = wheel
deps =
    wheel
    build
allowlist_externals =
    python3
commands =
    python3 -m build -w -o {toxinidir}/dist
    #python setup.py bdist_wheel -d {toxinidir}/wheelhouse

